query RunInput {
  paymentCustomization {
    metafield(namespace: "amount", key: "value") {
      value
    }
  }
  cart {
    cost {
      totalAmount {
        amount
      }
    }
  }
  paymentMethods {
    id
    name
  }
}
